<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/journalContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--Header-->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/journal_header_bg"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:id="@+id/txtHeaderTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            android:text="Private Journal"
            android:textSize="24sp"
            android:textStyle="bold"
            android:fontFamily="@font/poppins_bold"
            android:textColor="@android:color/white" />

        <TextView
            android:id="@+id/txtHeaderSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your personal space for thoughts and memories"
            android:textColor="@android:color/white"
            android:fontFamily="@font/poppins_italic"
            android:textSize="14sp"
            android:layout_marginTop="4dp" />
    </LinearLayout>

    <!-- Calendar/Journal View Toggle Buttons -->
    <LinearLayout
        android:id="@+id/viewToggle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="#FFFFFF"
        android:layout_marginTop="120dp">

        <Button
            android:id="@+id/btnCalendarView"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_margin="1dp"
            android:text="Calendar View"
            android:fontFamily="@font/poppins_semibold" />

        <Button
            android:id="@+id/btnJournalView"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_margin="1dp"
            android:text="Journal Entries"
            android:fontFamily="@font/poppins_semibold"/>
    </LinearLayout>

    <!--View container (calendar or journal fragment)-->

    <!--Floating Action Button-->
    <FrameLayout
        android:id="@+id/fabContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="180dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddEntry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:src="@drawable/ic_add_24"
        app:tint="@color/blue"
        app:backgroundTint="#F2E205"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
